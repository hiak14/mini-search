# mini-search
A small information retrieval engine in pure Python, no external IR libraries. It builds a compressed inverted index over a document collection and answers boolean, phrase, and ranked free-text queries with BM25 or TF-IDF.
